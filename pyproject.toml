[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "tmc-llm"
version = "0.1.0"
description = "TinyLlama LoRA training scaffold for Trinidad Municipal College knowledge."
requires-python = ">=3.11,<3.14"

[tool.setuptools.packages.find]
where = ["src"]

